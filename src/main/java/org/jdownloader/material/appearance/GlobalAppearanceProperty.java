package org.jdownloader.material.appearance;

/** Individually resettable fields in the persisted global appearance profile. */
public enum GlobalAppearanceProperty {
    THEME,
    DENSITY,
    SEED_COLOR,
    ACCENT_COLOR,
    FONT_FAMILY,
    FONT_SOURCE,
    FONT_SIZE_SCALE,
    FONT_WEIGHT,
    CJK_FALLBACK
}
