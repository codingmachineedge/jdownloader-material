package org.jdownloader.material.appearance;

/** Where the selected typeface came from. The family name remains portable either way. */
public enum FontSource {
    SYSTEM,
    INSTALLED,
    BUNDLED
}
