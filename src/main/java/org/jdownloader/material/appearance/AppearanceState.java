package org.jdownloader.material.appearance;

/** Interaction states that may have independent per-target appearance overrides. */
public enum AppearanceState {
    NORMAL,
    HOVER,
    FOCUSED,
    PRESSED,
    DISABLED,
    SELECTED,
    CHECKED,
    ERROR
}
