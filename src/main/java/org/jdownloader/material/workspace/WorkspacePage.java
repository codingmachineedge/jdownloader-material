package org.jdownloader.material.workspace;

/** Pages that can live in a persisted workspace tab. */
public enum WorkspacePage {
    DOWNLOADS,
    LINKGRABBER,
    HISTORY,
    SETTINGS,
    ADD_LINKS
}
