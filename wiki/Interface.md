# Interface

The mint/deep-teal Material 3 shell combines a global transfer toolbar and rail with a persistent
browser-style workspace.

| Region | Behavior |
| --- | --- |
| 52 px toolbar | Add Links, scheduler controls, plain/regex search, throughput, theme, clipboard monitoring and window controls. |
| 208/72 px navigation rail | Opens or focuses primary pages; collapses to named icons below 980 px. |
| Workspace | Separate pinned/regular tab regions, groups, horizontal scrolling, overflow, four searches and guarded bulk close. |
| Dense content | 62 px page headings, 48 px action/data rows and 34 px table headers. |
| 30 px status bar | Aggregate speed, running count, remaining bytes, retry state and latest local activity. |
| Corner overlays | Bottom-right non-blocking notification stack and eligible bottom-left dim-sum card. |

## Pages and search

Downloads, LinkGrabber, History and Settings remain primary rail destinations. New Tab also opens
Add Links, Notification Center, Changelog and installed-JDownloader Accounts, Plugins, Captcha,
Extraction, Scheduler, Connections, Remote Control, Automation and Logs pages.

Every search bar is plain text by default and owns an adjacent anchored RE2/J builder with guided
literals/classes/anchors/groups/alternation/quantifiers, flags, bounded sample text, syntax feedback,
live matches/captures and copy/export. Settings supplies all-settings and per-section fields; tabs
supply current-strip, per-group, group-name and master fields.

## Appearance and language

Right-click any registered element for **Edit appearance…**, Shift+right-click to open directly or
use <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>A</kbd> for the focused element. Global theme/density/seed/
font and per-element/state typography/color/geometry/spacing/icon overrides apply live, persist and
support presets, reset and import/export. The color control translates every documented notation and
the font control searches installed/bundled families with CJK fallback.

English, playful Hong Kong Cantonese and bilingual modes switch live. Independent English and
Cantonese funny sliders run from 1–5; the disclosure explains that voice styles every message,
including warnings/errors, while facts and consequences remain unchanged.

## Feedback, history and access

Information/success notifications auto-dismiss; warnings/errors persist. The Notification Center
keeps bounded searchable history. Changelog composes plain/regex search and typed/calendar date
ranges, then copies or exports the filtered Markdown view. The 1% dim-sum card is exactly-once per
eligible launch, focus-preserving and optional.

Tabs, groups, searches and appearance commands are keyboard reachable and have accessible names;
status color is reinforced with text/icon/value; bilingual copy wraps; reduced motion is respected.

Try the [tabbed interactive site](https://ding-ding-projects.github.io/jdownloader-material/) or
inspect the [design system](Design-System).

The interactive site uses eight discrete tabs and carries its own persisted tab manager,
pin/group/reorder/collapse actions, four independent discovery searches, previewed bulk close,
bounded ECMAScript builders, three language modes, independent 1–5 funny settings, notifications,
local dim sum and an M3 appearance workbench. Eleven searchable in-site articles keep feature
behavior, configuration, failures, security and verification beside the demo. Browsers cannot
enumerate every installed font, so the site honestly exposes free-entry local families, searchable
known choices and CJK-safe fallback.
